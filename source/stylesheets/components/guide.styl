@require 'includes/variables'

/*
local guide
====================================================================== */
.guide_place {
    display: block;

    &::before {
        content: ' > ';
        color: $berlin-headings;
        font-weight: bold;
    }
}
