/*
media elements
====================================================================== */

img, svg, video {
    max-width: 100%;
    height: auto;
}

iframe {
    margin-bottom: 1em;
}

svg {
    fill: currentColor;
    stroke: currentColor;
}
